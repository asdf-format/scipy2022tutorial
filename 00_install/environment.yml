name: asdf-tutorial-env

dependencies:
  - jupyter
  - notebook >= 6.0
  - matplotlib >= 3.2
  - numpy >= 1.16
  - packaging
  - pandas >= 1.0
  - pip
  - python >= 3.9
  - pyyaml
  - pip:
      - asdf
      - asdf-astropy
      - asdf-standard
      - astropy >= 4.2
      - gwcs >= 0.16
